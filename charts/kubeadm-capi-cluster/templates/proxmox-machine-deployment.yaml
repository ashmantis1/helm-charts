{{- if .Values.proxmoxWorkers.enabled }}
{{- range $key, $node := .Values.proxmoxWorkers.nodes }}
apiVersion: cluster.x-k8s.io/v1beta2
kind: MachineDeployment
metadata:
  name: {{ $.Values.cluster.name }}-proxmox-workers-{{ $node }}
spec:
  clusterName: {{ $.Values.cluster.name }}
  replicas: 1
  selector:
    matchLabels: null
  template:
    metadata:
      labels:
        cluster.x-k8s.io/cluster-name: {{ $.Values.cluster.name }}
        node-role.kubernetes.io/node: ""
    spec:
      bootstrap:
        configRef:
          apiGroup: bootstrap.cluster.x-k8s.io
          kind: KubeadmConfigTemplate
          name: {{ $.Values.cluster.name }}-proxmox-worker
      clusterName: svc
      infrastructureRef:
        apiGroup: infrastructure.cluster.x-k8s.io
        kind: ProxmoxMachineTemplate
        name: proxmox-worker-{{ $node }}
      version: {{ $.Values.kubernetes.version }}
---
{{- end }}
{{- end }}
